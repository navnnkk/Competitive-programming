# cook your dish here
t = int(input())

while t > 0:
    a,b = map(int,input().split())
    if b > a:
        print('cannot distribute')
    else:
        if b!=0:
            print(a // b)
        else:
            print("cannot distribute")
    t -= 1
